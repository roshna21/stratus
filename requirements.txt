# The reasoning layer
google-genai>=1.0        # default: free tier, no card needed
anthropic>=0.69          # optional: paid

# Web interface
fastapi>=0.115
uvicorn>=0.32

# Reaching the public Azure price list (no account or key required)
httpx>=0.27

# Data models and validation
pydantic>=2.9

# Azure: authentication, listing what exists, and creating the storage
# account that holds Terraform's state
azure-identity>=1.19
azure-mgmt-resource>=23.1
azure-mgmt-storage>=21.2

# Loads settings from a local .env file that is never committed
python-dotenv>=1.0
